#ifndef _MENU_
#define _MENU_

#include <SDL2/SDL.h>

#include "ships.h"

// I update today (paq1)
void menu(int *, SDL_Renderer *);

//Choices in the menu
void choices(int ,ship_t *, ship_t *);

#endif
